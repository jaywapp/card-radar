import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder smoke test', (WidgetTester tester) async {
    // Placeholder — full app smoke tests require Hive/router setup.
    expect(true, isTrue);
  });
}
